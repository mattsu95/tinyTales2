falas = []; 
pagina_atual = 0;       
caractere_atual = 0;    
velocidade_texto = 0.5; 

// Cronômetro para o controle de tempo opcional
timer_auto_sumir = 0;

// Força a GUI a ser pequena e nítida (perfeita para pixel art!)
display_set_gui_size(490, 270);
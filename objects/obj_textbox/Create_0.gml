falas = []; 
pagina_atual = 0;       
caractere_atual = 0;    
velocidade_texto = 0.5; 

// Cronômetro para o controle de tempo opcional
timer_auto_sumir = 0;

// Trava opcional para evitar que pulo/ataque feche tutoriais (padrão é false)
ignorar_inputs = false; 

// Força a GUI a ser pequena e nítida
display_set_gui_size(490, 270);